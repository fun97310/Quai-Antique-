
    <h1>La capacité du jour : {{ jour.jour }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.capacite) }}
        <button class="btn" id="editsubmit">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

