<form method="post" action="{{ path('app_type_plats_delete', {'id': typeplat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeplat.id) }}">
    <button class="btn">Delete</button>
</form>
