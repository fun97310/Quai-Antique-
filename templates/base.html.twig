<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Une gastronomie sans artifice - le Quai Antique{% endblock %}</title>
        <meta name="description" content="Le Quai Antique une expérience gastronomique, à travers une cuisine sans artifice. Comme une promesse d/'un voyage dans son univers culinaire.">
        <meta name="keywords" content="Gastronomie, Restaurant, Expérience, Culinaire, Chambéry, Producteur ">
        <meta name="robots" content="index, nofollow">
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
        <meta name="language" content="French">  
        <link rel="icon" type="image/png" href="{{ asset('./img/logo.png') }}">
        

        {% block stylesheets %}
          
        {% endblock %}      
        <link href="{{ asset('css/style1.css') }}" rel="stylesheet"/> 
    </head>
    <body>
        <header>
            <nav>
              {% if is_granted('ROLE_ADMIN') %}
                <a href='{{ path('app_admin') }}' id='logo' class="logo">Quai Antique</a>
              {% else %}   
                <a href='{{ path('app_acceuil') }}' id='logo' class="logo">Quai Antique</a> 
              {% endif %}
              <button class="close" id='closebtn'>X</button>
              <ul id='nav'>
                <li><a href='{{ path('app_acceuil') }}'>Acceuil</a></li>
                <li><a href='{{ path('app_carte') }}'>La Carte</a></li>
                <li><a href="{{ path('app_reservation_new') }}">Réserver</a></li> 
                <li><button class="connexion" >
                  {% if is_granted('ROLE_USER') %}
                    <a href=" {{ path('app_account') }} ">Mon Compte</a>
                  {% else %}
                    <a href=" {{ path('app_login') }} ">Connexion</a>
                  {% endif %}
                </button></li>
              </ul>
              <img src="{{ asset('img/menu.png') }}" alt='icon menu' id='navbtn'>
            </nav>
        </header>
        {% block body %}

        {% endblock %}
        <footer>
          <div class="horaire">
            {{ include('horaire/horaire.html.twig') }}
          </div>
          <div class="footer-link">
            <a href="#">Acceuil</a>
            <div class="footer-ligne"></div>
            <a href="#">Contact</a>
            <div class="footer-ligne"></div>
            <a href="#">Mentions legales</a>
            <div class="footer-ligne"></div>
            <a href="#">32 avenue des reblochons 73000 Chambèry</a>
            <div class="footer-ligne"></div>
           
          </div>  
        </footer>
        <script src="{{ asset('js/index.js') }}"></script>
        {% block javascripts %}
          
        {% endblock %}
        
        
    </body>
</html>
