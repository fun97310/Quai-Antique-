<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>  
        {% block stylesheets %}
          
        {% endblock %}      
        <link href="{{ asset('css/style1.css') }}" rel="stylesheet"/> 
    </head>
    <body>
        <header>
            <nav>
              {% if is_granted('ROLE_ADMIN') %}
                <a href='{{ path('app_admin') }}' id='logo' class="logo">Quai Antique</a>
              {% else %}   
                <a href='{{ path('app_acceuil') }}' id='logo' class="logo">Quai Antique</a> 
              {% endif %}
              <button class="close" id='closebtn'>X</button>
              <ul id='nav'>
                <li><a href='{{ path('app_acceuil') }}'>Acceuil</a></li>
                <li><a href='{{ path('app_carte') }}'>La Carte</a></li>
                <li><a href="{{ path('app_reservation_new') }}">Reserver!</a></li> 
                <li> <a href="{{ path('app_menus_index') }}"></a></li> 
                <li><button class="connexion" >
                  {% if is_granted('ROLE_USER') %}
                    <a href=" {{ path('app_account') }} ">Mon Compte</a>
                  {% else %}
                    <a href=" {{ path('app_register') }} ">S'inscrire</a>
                  {% endif %}
                </button></li>
              </ul>
              <img src="{{ asset('img/menu.png') }}" alt='icon menu' id='navbtn'>
            </nav>
        </header>
        {% block body %}

        {% endblock %}
        <footer>
          <div class="horaire">
            {{ include('horaire/horaire.html.twig') }}
          </div>
          <div class="footer-link">
            <a href="#">Acceuil</a>
            <div class="footer-ligne"></div>
            <a href="#">Contact</a>
            <div class="footer-ligne"></div>
            <a href="#">Mention legale</a>
            <div class="footer-ligne"></div>
            <a href="#">32 avenue des reblochons 73000 Chambèry</a>
            <div class="footer-ligne"></div>
           
          </div>  
        </footer>
        <script src="{{ asset('js/index.js') }}"></script>
        <script src="{{ asset('js/horaire.js') }}"></script>
        <script src="{{ asset('js/requestAjax.js') }}">
          renderHoraires(jours);
        </script>
        <script>
          
        </script>
    </body>
</html>
{#
  <nav>
              <a href='{{ path('app_home') }}' id='logo' class="logo">Quai Antique</a>
              <button class="close" id='closebtn'>X</button>
              <ul id='nav'>
                <li><a href='{{ path('app_horaire') }}'>Acceuil</a></li>
                <li><a href='{{ path('app_images_index') }}'>Acceuil</a></li>
                <li><a href="{{ path('app_carte') }}">carte</a></li> 
                <li> <a href="{{ path('app_menus_index') }}">Menu</a></li> 
                <li><button class="connexion" >
                  {% if is_granted('ROLE_USER') %}
                    <a href=" {{ path('app_login') }} ">Mon Compte</a>
                  {% else %}
                    <a href=" {{ path('app_login') }} ">Se connecter</a>
                  {% endif %}
                </button></li>
              </ul>
              <img src="{{ asset('img/menu.png') }}" alt='icon menu' id='navbtn'>
            </nav>
#}