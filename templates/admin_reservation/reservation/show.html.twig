{% extends 'base.html.twig' %}

{% block title %}Quai Antique - Votre reservation{% endblock %}

{% block body %}
    <h1>Réservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ reservation.email }}</td>
            </tr>
            <tr>
                <th>Nmbr_couvert</th>
                <td>{{ reservation.nmbrCouvert }}</td>
            </tr>
            <tr>
                <th>Allergie</th>
                <td>{{ reservation.allergie }}</td>
            </tr>
            <tr>
                <th>Heure</th>
                <td>{{ reservation.heure }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_acceuil') }}">retour a la page d'acceuil</a>

    <a href="{{ path('app_reservation_edit', {'id': reservation.id}) }}">modifier ma reservation</a>

    {{ include('admin_reservation/reservation/_delete_form.html.twig') }}
{% endblock %}
